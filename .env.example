# --- Frontend (used by the React app in the browser) ---
VITE_GROUP_CODE=หนูดีทีมเก่ง2569

# --- Backend (used only by api/leaderboard.js on Vercel's server — never sent to the browser) ---
# Create a GitHub fine-grained Personal Access Token at:
#   https://github.com/settings/tokens?type=beta
# Give it "Contents: Read and write" permission on ONLY this one repo.
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxx
GITHUB_REPO=your-username/exam-prep-app
GITHUB_BRANCH=main
GITHUB_FILE_PATH=data/leaderboard.json
